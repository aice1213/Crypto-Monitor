# Cloudflare Worker 的 API 地址（含 /api 路径前缀）
# 生产环境使用自定义域名路由（绕过 workers.dev 屏蔽）
VITE_API_BASE=https://smartfield.dpdns.org/api

# 可选：开发时自定义 vite 代理的 Worker 地址（默认 http://localhost:8787）
# VITE_WORKER_URL=http://localhost:8787
